import glob
import os
import pathlib
import subprocess
from environment_handler import Handler
"""
print("Begin to generate the experiments")

SCRIPTS_FOLDER_FILES = '/scripts/*.py'

main_script_path = os.path.dirname(os.path.abspath(__file__))

scripts = glob.glob(main_script_path + SCRIPTS_FOLDER_FILES)

for script in scripts:
    print(script)
    os.system('python3 '+script)
"""



print(command)
